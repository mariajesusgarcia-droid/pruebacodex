import Button from './Button'
import { buttonArgTypes, buttonArgs } from './buttonStoryShared'

const meta = {
  title: 'Components/Button/Playground',
  component: Button,
  parameters: {
    layout: 'centered',
  },
  args: buttonArgs,
  argTypes: buttonArgTypes,
}

export default meta

export const Interactive = {}
